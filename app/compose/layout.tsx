import type { ReactNode } from 'react';

export const metadata = {
    title: 'Compose | ZUNNO',
    description: 'Draft and schedule social posts',
};

export default function ComposeLayout({ children }: { children: ReactNode }) {
    return children;
}
